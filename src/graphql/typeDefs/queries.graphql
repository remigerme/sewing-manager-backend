type Query {
  tagCategories: [TagCategory!]!
  tags(category: TagCategory!): [Tag!]!

  tissue(id: Int!): Tissue
  pattern(id: Int!): Pattern
  production(id: Int!): Production
  accessory(id: Int!): Accessory

  search(type: SewingItemType, tag: Tag): [SewingItem!]!
}
